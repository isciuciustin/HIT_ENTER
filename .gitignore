# Rust
/target
**/*.rs.bk

# Node
node_modules/
web/dist/
.vite/

# Tauri
src-tauri/target/
src-tauri/gen/

# Local databases — never commit chat history or server identity keys
*.db
*.db-shm
*.db-wal
/data
/he-data

# Device keys. `he-cli` writes ./he-cli.key by default, and it is a private key:
# whoever has it is that enrolled device.
*.key

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
