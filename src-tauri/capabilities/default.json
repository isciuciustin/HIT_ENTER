{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the HIT_ENTER window is allowed to ask the Rust side for. Tauri v2 denies by default, so anything not listed here is not reachable from the frontend \u2014 which is the point.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "deep-link:default",
    "clipboard-manager:allow-write-text"
  ]
}
